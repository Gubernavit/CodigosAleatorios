CREATE TABLE USUARIO (
    ID INT,
    NICKNAME VARVHAR(100)PRIMARY KEY,
    EMAIL VARCHAR(100),
    SENHA INT,
    DATA_NASC INT,
    GENERO VARCHAR(100)
);

INSERT INTO USUARIO (ID, NICKNAME, SENHA, EMAIL, DATA_NASC, GENERO)
    VALUES (0, "ThataHD", 12345, "Thalissonrodriguesrocha08@gmail.com", 12042008, "Masculino");
    
SELECT * FROM USUARIO;



CREATE TABLE PERSONAGEM (
    ID INT PRIMARY KEY,
    VIDA REAL,
    PONTUACAO REAL,
    STAMINA REAL,
    INTELIGENCIA REAL,
    ARMADURA REAL,
    PERSOASAO REAL,
    ADRENALINA REAL,
    SORTE REAL,
    PERCEPCAO REAL,
    CONQUISTAS INT
);

INSERT INTO PERSONAGEM (ID, VIDA, PONTUACAO, STAMINA, INTELIGENCIA, ARMADURA, PERSOASAO, ADRENALINA, SORTE, PERCEPCAO, CONQUISTAS)
    VALUES (0, 1000, 86, 70, 47, 52, 90, 15, 20, 69, 9);

SELECT * FROM PERSONAGEM;



CREATE TABLE MAPA (
    ID INT PRIMARY KEY,
    CONQUISTAS INT,
    TEMPO_DE_JOGO REAL,
    MORTES INT,
    ESTILO_DE_JOGO CHARVAR(100),
    DISTANCIA_PERCORRIDA REAL,
    DIFICULDADE INT
);

INSERT INTO MAPA (ID, CONQUISTAS, TEMPO_DE_JOGO, MORTES, ESTILO_DE_JOGO, DISTANCIA_PERCORRIDA, DIFICULDADE)
    VALUES (0, 3, 6, 207, "FURTIVO", 14752, 2);
    
SELECT * FROM MAPA;



CREATE TABLE NPC (
    ID INT PRIMARY KEY,
    TIPO CHARVAR(100)
);

INSERT INTO NPC (ID, TIPO)
    VALUES (0, "NEGOCIANTE");
    
SELECT * FROM NPC;



CREATE TABLE INIMIGO (
    ID INT PRIMARY KEY,
    TIPO CHARVAR(100),
    VIDA REAL
);

INSERT INTO INIMIGO (ID, TIPO, VIDA)
    VALUES (0, "BOSS 1", 100);
    
SELECT * FROM INIMIGO;



SELECT * FROM USUARIO;
SELECT USUARIO.NICKNAME, PERSONAGEM.ID FROM USUARIO, PERSONAGEM;

DROP TABLE USUARIO;
DROP TABLE PERSONAGEM;
DROP TABLE MAPA;
DROP TABLE NPC;
DROP TABLE INIMIGO;

/*ALTER TABLE USUARIO ADD coluna
    

    


